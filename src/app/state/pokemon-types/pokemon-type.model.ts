import { ID } from '@datorama/akita';

export interface PokemonType {
  id: ID;
  name: string;
  url: string;
}

// export function createPokemonType(params: Partial<PokemonType>) {
//   return {

//   } as PokemonType;
// }
